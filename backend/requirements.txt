# ============================================================
#  REQGEN — Python Dependencies
#  Install: pip install -r requirements.txt
# ============================================================

# ---- WEB FRAMEWORK ----
fastapi==0.111.0
uvicorn[standard]==0.29.0          # ASGI server to run FastAPI

# ---- DATABASE ----
sqlalchemy==2.0.30                 # ORM
alembic==1.13.1                    # DB migrations
psycopg2-binary==2.9.9             # PostgreSQL driver

# ---- ENVIRONMENT ----
python-dotenv==1.0.1               # Load .env files

# # ---- AUTHENTICATION ----
passlib[bcrypt]==1.7.4             # Password hashing
python-jose[cryptography]==3.3.0   # JWT tokens

# ---- VALIDATION ----
pydantic==2.7.1                    # Data validation (built into FastAPI)
pydantic-settings==2.2.1          # Settings management via env vars
# email-validator==2.1.1             # Email field validation


# # ---- NLP — HEAVY (use HuggingFace Spaces for inference) ----
transformers==4.41.0               # HuggingFace Transformers
torch==2.3.0                       # PyTorch (CPU version — see note below)
# # NOTE: For CPU-only install use:
# # pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cpu

# # ---- HTTP CLIENT (to call HuggingFace Spaces) ----
httpx==0.27.0                      # Async HTTP client for calling HF Space API

# # ---- EXPORT ----
python-docx==1.1.2                 # Export to Word (.docx)
openpyxl==3.1.2                    # Export to Excel (.xlsx)

# # ---- UTILITIES ----
# python-multipart==0.0.9            # File upload support in FastAPI

# # ============================================================
# #  NOT YET INSTALLED (plug in later)
# # ============================================================
# # rasa                             # Install in a SEPARATE virtual environment
# #                                  # Rasa has dependency conflicts with the above

