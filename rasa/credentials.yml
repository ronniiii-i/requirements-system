rest:
  # No auth needed for local dev.
  # The React frontend will call POST /webhooks/rest/webhook

socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true